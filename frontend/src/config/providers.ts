/**
 * Frontend Provider Configuration
 *
 * Feature flags controlling which content providers are active.
 * Must stay in sync with backend config.ts provider flags.
 */

export const providerConfig = {
    youtube:   true,
    twitter:   true,
    instagram: true,
    github:    true,
    medium:    true,
    notion:    true,
};
